interface:
  display_name: "ExcelT"
  short_description: "Excel형 웹 대시보드 Shell·입력 UX·테마 구현"
  brand_color: "#107C41"
  default_prompt: "Use $excelT to transform or build this dashboard as an Excel-like web application with familiar number input, table navigation, formulas, ExcelKospi styling, top/bottom application chrome, multi-level tabs, and a persistent theme picker when the reference calls for it."

policy:
  allow_implicit_invocation: true
